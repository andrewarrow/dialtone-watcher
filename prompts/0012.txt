review ~/dev/dialtoneapp/src/pages/Demo/index.jsx for what the goal in data collection is. And look at next_steps.md as well. Look at the current state of data we have now when running ./dialtone-watcher summary and let's come up with the right json to send to a POST to https://dialtoneapp.com/api/v1/watcher with the machine_id in the header. Include the hardware profile (Apple M4 Max, 36.0 GB RAM, 14 logical cores) for example, normalized think of a good format for this. Also include the summary data for a certain period of time. When testing lets send it often but make this configurable so in production mode it will send it much less often. Think about the right format and size of each post. The goal is to have 20000 machines all running this and sending their data. Let's not overwhelm the backend with too much data but make sure we send enough to make the ~/dev/dialtoneapp/src/pages/Demo/index.jsx demo become real. Don't worry about next_steps.md data yet but think about how we will eventually send more info like this.  
